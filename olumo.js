const olumoMenu = document.getElementById('olumo-menu')
const olumoButton = document.getElementById ('olumo-button')
olumoButton.addEventListener('click', () => {
    olumoMenu.style.display = olumoMenu.style.display
    === 'block' ? 'block' : 'block';
});


